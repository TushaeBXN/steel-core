apiVersion: v1
kind: ConfigMap
metadata:
  name: steelcore-config
  namespace: steelcore
data:
  NODE_ENV: "production"
  REDIS_URL: "redis://:${REDIS_PASSWORD}@redis-service:6379"
  DATABASE_URL: "postgresql://steelcore:${DB_PASSWORD}@postgres-service:5432/steelcore"
  S3_BUCKET: "steelcore-sdks"
